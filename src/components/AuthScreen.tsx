import { useState } from 'react';
import { ShieldCheck, Bot } from 'lucide-react';
import { useAuth } from '../lib/auth';
export function AuthScreen(){const{login,register}=useAuth();const[mode,setMode]=useState<'login'|'register'>('login');const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{if(mode==='login'){await login(email,password);}else{await register(name,email,password);}}catch(err){setError(err instanceof Error?err.message:'Authentication failed');}finally{setBusy(false);}}return <div className="min-h-screen bg-slate-950 text-white flex items-center justify-center p-6"><div className="w-full max-w-md rounded-2xl border border-cyan-500/30 bg-slate-900/90 p-8 shadow-2xl"><div className="flex items-center gap-3 mb-7"><div className="rounded-xl bg-cyan-500/15 p-3"><Bot className="text-cyan-300"/></div><div><h1 className="text-2xl font-bold">JERVIS</h1><p className="text-sm text-slate-400">Secure Device Intelligence</p></div></div><div className="grid grid-cols-2 bg-slate-800 rounded-lg p-1 mb-6"><button className={`p-2 rounded-md ${mode==='login'?'bg-cyan-600':''}`} onClick={()=>setMode('login')}>Login</button><button className={`p-2 rounded-md ${mode==='register'?'bg-cyan-600':''}`} onClick={()=>setMode('register')}>Register</button></div><form onSubmit={submit} className="space-y-4">{mode==='register'&&<input className="w-full rounded-lg bg-slate-800 border border-slate-700 p-3" placeholder="Full name" value={name} onChange={e=>setName(e.target.value)} required/>}<input className="w-full rounded-lg bg-slate-800 border border-slate-700 p-3" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="w-full rounded-lg bg-slate-800 border border-slate-700 p-3" type="password" placeholder="Password (minimum 8 characters)" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/>{error&&<p className="text-red-400 text-sm">{error}</p>}<button disabled={busy} className="w-full rounded-lg bg-cyan-600 hover:bg-cyan-500 p-3 font-semibold disabled:opacity-50">{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button></form><div className="mt-6 flex gap-2 text-xs text-slate-400"><ShieldCheck size={16}/><span>The first registered account becomes administrator. All devices and records are owner-scoped.</span></div></div></div>}
